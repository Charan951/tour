include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat accidental production logging and context misuse as warnings so
    # they show up in `flutter analyze` output.
    avoid_print: warning
    use_build_context_synchronously: warning
